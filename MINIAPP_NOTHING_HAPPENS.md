# Mini App bad_hash — নিরপেক্ষ বিশ্লেষণ ও সমাধান

তারিখ: 2026-08-16 · ব্রাঞ্চ: `arena/01a009d6-claude`

---

## প্রথমে সোজা কথা: আমার আগের দুটো উত্তরই ভুল ছিল

আপনি ঠিকই বলেছেন। নিরপেক্ষভাবে স্বীকার করছি:

| # | আমি যা বলেছিলাম | কেন ভুল |
|---|---|---|
| ১ | "টোকেন পুরনো, আবার কপি করুন" | শর্তটা ছিল **tautology** — নিজের টোকেনের id নিজের সাথেই মেলানো |
| ২ | "পুরনো বটের পুরনো মেসেজের বাটন" | `query_id` শুধু বলে **কোথা থেকে** খোলা হয়েছে, **কোন বট সাইন করেছে** তা নয় |

দুটোই ছিল **অনুমান, প্রমাণ নয়** — কিন্তু সার্ভার সেগুলো নিশ্চিত সত্যের মতো
ছাপছিল। আপনি নতুন বট বানালেন, সঠিক বট খুললেন — দুবারই কিছু বদলায়নি। **সমস্যা
আপনার দিকে ছিল না, আমার নির্ণয়ে ছিল।**

---

## আপনার `/health` যা প্রমাণ করে

```json
"telegram_bot":          {"username":"myAutomaitBot","id":8221572217}
"telegram_token_source": {"set":["BOT_TOKEN"],"conflict":false}
"miniapp_url":           {"ok":true}
```

এটা দিয়ে **নাকচ** হয়ে যায়:

| সম্ভাবনা | রায় | কারণ |
|---|---|---|
| ভুল বট খোলা হয়েছে | ❌ নাকচ | getMe বলছে টোকেনটা `@myAutomaitBot`-এরই, আপনি ওটাই খুলেছেন |
| দুই env var-এ দুই বট | ❌ নাকচ | `conflict: false`, শুধু `BOT_TOKEN` সেট |
| HMAC কোড ভুল | ❌ নাকচ | Telegram-এর **অফিসিয়াল টেস্ট ভেক্টর** পাস করে (চালিয়ে দেখেছি) |
| পুরনো পেলোড replay | ❌ নাকচ | `age_s=3` |
| কোনো ফিল্ডের decoding আলাদা | ❌ নাকচ | `culprit=None` |

বাকি থাকে **একটাই**: সার্ভারে থাকা টোকেনের **secret অংশ** সেটা নয় যেটা দিয়ে
Telegram সাইন করছে।

---

## কিন্তু getMe তো সফল! — এখানেই আসল ফাঁদ

`_bot_identity()` **বুটে একবার** getMe চালিয়ে ফল **cache** করে, আর কখনো
রিফ্রেশ করে না। তাই:

- বুটের পর ড্যাশবোর্ডে টোকেন বদলালে → `/health` পুরনো পরিচয়ই দেখাতে থাকে
- বুটের সময় Telegram unreachable থাকলে → একই সমস্যা

**অর্থাৎ `/health`-এর `ok` প্রমাণ নয়।** ঠিক এভাবেই সব সবুজ দেখায় অথচ প্রতিটা
সাইন-ইন ব্যর্থ হয়।

---

## সমাধান: অনুমান বাদ, প্রমাণ

Telegram initData **দুবার** সাইন করে:

| ফিল্ড | কী দিয়ে | কে যাচাই করতে পারে |
|---|---|---|
| `hash` | আপনার **বট টোকেন** (HMAC) | শুধু আপনি |
| `signature` | **Telegram-এর নিজের Ed25519 key** | **যে কেউ — শুধু public bot id লাগে** |

এটাই নির্ণায়ক, কারণ এতে বট টোকেন লাগে **না**:

- `signature` ✅ + `hash` ❌ → পেলোড **আসল** ও **এই bot id-র জন্যই** →
  **আমাদের টোকেনের secret ভুল** (প্রমাণিত, অনুমান নয়)
- `signature` ❌ → পেলোড এই বটের জন্য সাইন করাই হয়নি

যেখানে `signature` নেই (পুরনো ক্লায়েন্ট), সেখানে **তখনই লাইভ getMe** চালানো
হয় (cache নয়) — 401 এলে সেটাই চূড়ান্ত উত্তর। Telegram একটা বটের জন্য **একসাথে
একটাই** টোকেন সক্রিয় রাখে, তাই এটা নিশ্চিত।

**Telegram unreachable হলে কোনো রায় দেওয়া হয় না** — নিজেদের নেটওয়ার্ক সমস্যার
দোষ আপনার টোকেনের ঘাড়ে চাপানো হবে না।

---

## ✅ এখন যা করবেন

**১. আপডেট ডিপ্লয় করুন** (এই ব্রাঞ্চ), তারপর `/health` দেখুন:

```json
"telegram_token_live": { "ok": true }    ← এটাই আসল উত্তর, cache নয়
```

- `ok: false` → **টোকেনই দোষী, প্রমাণিত**। @BotFather → /mybots → বট →
  API Token → **Revoke current token** → নতুনটা `BOT_TOKEN`-এ বসান
- `ok: true` → টোকেন ঠিক আছে; পরের সাইন-ইন চেষ্টার লগ দেখুন

**২. Mini App আবার খুলুন**, তারপর Render লগে এই লাইনটা দেখুন:

```
miniapp bad_hash: ... tg_signature=VALID  token_live=True
```

- `tg_signature=VALID` → **secret ভুল, নিশ্চিত** — Revoke করে নতুন টোকেন দিন
- `tg_signature=not_signed_for_this_bot` → পেলোড অন্য বটের
- `token_live=False` → Telegram টোকেন reject করেছে

এখন আর অনুমান করতে হবে না — লগ সরাসরি বলে দেবে।

> **সবচেয়ে সম্ভাব্য:** `Revoke current token` চেপে **নতুন** টোকেন নেওয়া।
> নতুন বট বানানোর দরকার নেই — একই `@myAutomaitBot`-এর টোকেন রিফ্রেশ করাই যথেষ্ট।
> কারণ getMe পাস করা টোকেনও সেই টোকেন হতে পারে যা দিয়ে Telegram আর সাইন করছে না।

---

## যা বদলেছে

| ফাইল | পরিবর্তন |
|---|---|
| `services/miniapp_auth.py` | নতুন `third_party_check()` — Telegram-এর Ed25519 signature যাচাই (বট টোকেন ছাড়াই); নতুন `token_live()` — লাইভ getMe, ৬০s cache, টোকেন বদলালেই invalidate |
| `routes/auth.py` | রায় এখন **প্রমাণভিত্তিক**; লগে `tg_signature=` ও `token_live=`; unreachable হলে কোনো দোষারোপ নয় |
| `app.py` | `/health` → `telegram_token_live` (cache-মুক্ত) |
| `requirements.txt` | `cryptography` (শুধু নির্ণয়ের জন্য, লগইন গ্রহণে নয়) |
| `tests/test_badhash_evidence.py` | **নতুন** — ২৩টি টেস্ট |

### নিরাপত্তা অক্ষত
Ed25519 চেক **কখনোই লগইন গ্রহণ করে না** — শুধু ব্যর্থতা ব্যাখ্যা করে।
টেস্টে যাচাই: সঠিক পেলোড 200, tampered পেলোড 400, junk-এ কিছুই ফাঁস হয় না।

## টেস্ট ফলাফল

```
test_badhash_evidence  23 passed,  0 failed   (নতুন — পুরনো কোডে চলেই না)
test_badhash_verdict   24 passed,  0 failed
test_miniapp_opens     33 passed,  0 failed
test_all_routes        65 pass,    0 fail
test_web_batch         24 pass,    0 fail
test_security_batch    22 pass,    0 fail
test_miniapp_auth     127 passed,  3 failed   (বেসলাইন — /code সংক্রান্ত)
test_telegram_link     78 passed,  1 failed   (বেসলাইন)
```

---

## "প্রথম থেকে শুরু করব?" — আমার সৎ মতামত: **না**

কারণগুলো প্রমাণসহ:

- **অ্যাপ ভাঙা নয়।** HMAC অফিসিয়াল ভেক্টরে পাস, `/health` সব সবুজ, বট চলছে,
  `miniapp_url` ঠিক। ব্যর্থ হচ্ছে **একটি মান** — টোকেনের secret।
- **নতুন করে লিখলে একই জায়গায় আটকাবেন**, কারণ সমস্যাটা কোডের গঠনে নয়।
  আগের বার নতুন বট বানিয়েও লাভ হয়নি — সেটাই প্রমাণ।
- **যা সত্যিই অভাব ছিল** — নির্ণয়ের প্রমাণ — সেটা এখন যোগ করা হয়েছে।

আগে `/health`-এর `telegram_token_live` আর নতুন লগ লাইনটা দেখুন। ওটা এক
লাইনেই বলে দেবে সমস্যা কোথায়। তারপরও যদি না মেলে, ওই দুটো আউটপুট পাঠান —
তখন আর অনুমান করতে হবে না।
