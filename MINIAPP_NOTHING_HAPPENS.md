# সত্যিটা কী — Mini App bad_hash

তারিখ: 2026-08-16 · ব্রাঞ্চ: `arena/01a009d6-claude`

---

## এক লাইনে সত্য

> **Render-এ যে সার্ভিসটা চলছে, সেটা `BOT_TOKEN`-এ যে মান পড়ছে, সেটা এখন
> BotFather-এ থাকা টোকেন নয় — যদিও দুটোই "বৈধ" দেখায়।**
>
> নতুন বট বানিয়ে লাভ হয়নি, কারণ সমস্যা বট নয়। **টোকেন আবার revoke করেও লাভ
> হবে না** — সেটা তৃতীয় একটা টোকেন বানাবে, আর সেটাও একই ভুল জায়গায় গিয়ে বসবে।

---

## কীভাবে নিশ্চিত হলাম — আপনার নিজের লগ থেকে

```
tg_signature=VALID    token_live=None
```

`tg_signature=VALID` মানে Telegram-এর **নিজের Ed25519 স্বাক্ষর** যাচাই হয়েছে।
এই যাচাইয়ে **বট টোকেন লাগে না** — শুধু public bot id লাগে। এটা যা প্রমাণ করে:

| সম্ভাব্য কারণ | রায় |
|---|---|
| পেলোড জাল | ❌ বাদ |
| অন্য বটের পেলোড / পুরনো মেসেজের বাটন | ❌ বাদ |
| `photo_url`-এর `\/` escape সমস্যা | ❌ বাদ |
| ফিল্ড decoding / দুবার URL-decode | ❌ বাদ |
| আমাদের HMAC কোড ভুল | ❌ বাদ (অফিসিয়াল টেস্ট ভেক্টরও পাস করে) |

কারণ Ed25519 আর HMAC **একই বাইটের উপর** যাচাই হয় (শুধু bot-id prefix আলাদা),
আর দুটোই একই parse ব্যবহার করে। তাই স্বাক্ষর মিলেছে মানে **ডেটা byte-perfect**।

বাকি থাকে **একটিমাত্র ইনপুট: টোকেনের secret অংশ।**

---

## কিন্তু getMe তো টোকেন মেনে নেয়!

এটাই আসল সূত্র, এবং এখানেই আমি আগের বার ভুল করেছিলাম।

- Telegram একটা বটের জন্য **একসাথে একটাই** টোকেন বৈধ রাখে
- getMe বলছে আমাদের টোকেন **বৈধ**
- অথচ Telegram **অন্য secret** দিয়ে সাইন করছে

দুটো একসাথে সত্য হতে পারে **একটিমাত্র শর্তে** — সার্ভার প্রসেস যে মান **লোড
করেছে**, সেটা BotFather-এ এখন যা আছে তা নয়। মানে টোকেনটা একসময় বৈধ ছিল, বা
অন্য কোথাও থেকে আসছে।

### সবচেয়ে সম্ভাব্য কারণ (এই ক্রমে দেখুন)

1. **অন্য env var / Environment Group** `BOT_TOKEN`-কে override করছে
2. **`render.yaml` / Blueprint / Env Group**-এ পুরনো মান pin করা, যা প্রতি
   deploy-এ আবার বসে যায়
3. **সার্ভিস আসলে restart হয়নি** — এই প্রসেস `BOT_TOKEN` **একবারই** পড়ে,
   import-এর সময়
4. **অসম্পূর্ণ paste** — টোকেন কেটে গেছে (তখনও getMe পাস করতে পারে)

---

## `token_live=None` কেন ছিল — আমার আরেকটা ফাঁক

লাইভ getMe চেকটা `if not _tp.get("ok")` শর্তে বাঁধা ছিল। আপনার ক্ষেত্রে
`tg_signature=VALID` তাই **চেকটা কখনো চলেইনি** → `None`।

কিন্তু ওই উত্তরটাই সবচেয়ে জরুরি ছিল:

| token_live | মানে | সঠিক কাজ |
|---|---|---|
| `false` | Telegram টোকেন **reject** করছে (মৃত) | BotFather থেকে **কপি** করুন |
| `true` | টোকেন বৈধ, তবু অন্য secret দিয়ে সাইন | **override / restart খুঁজুন** — revoke নয় |

দুটোর কাজ সম্পূর্ণ **উল্টো**। এটা না জেনে আমি "Revoke current token" বলেছিলাম —
আপনার ক্ষেত্রে **সেটাই ভুল পরামর্শ ছিল**। ঠিক করা হয়েছে: চেকটা এখন সবসময় চলে।

---

## ✅ এখন যা করবেন — অনুমান ছাড়া, ৩ ধাপ

**১. ডিপ্লয় করুন, তারপর `/health` খুলুন:**

```json
"telegram_token_fingerprint": {
  "sha256_12": "xxxxxxxxxxxx",     ← যে টোকেন সার্ভার আসলে চালাচ্ছে
  "secret_length": 35
}
```

**২. BotFather-এর টোকেনের digest বের করুন** (টোকেন কাউকে দেখাতে হবে না):

```bash
printf '%s' '8221572217:আপনার-আসল-টোকেন' | sha256sum
```

প্রথম **১২ অক্ষর** মেলান:

| ফলাফল | অর্থ | করণীয় |
|---|---|---|
| **আলাদা** ← সবচেয়ে সম্ভাব্য | সার্ভার **ভুল মান** চালাচ্ছে | উপরের ৪টা কারণ খুঁজুন |
| **এক** | সঠিক মান ডিপ্লয়েড | `telegram_token_live` দেখুন |

`secret_length` ৩৫ না হলে → **paste কেটে গেছে**, পুরোটা আবার কপি করুন।

**৩. এখনো না মিললে**, Mini App খুলে নতুন লগ লাইনটা পাঠান — সেটা এখন
`TOKEN IS VALID BUT IS NOT THE SIGNING TOKEN` বলে ৪টা কারণ ক্রম অনুযায়ী
তালিকা করে দেবে।

> 💡 **শর্টকাট:** Render-এ Environment থেকে `BOT_TOKEN` **মুছে**, নতুন করে
> যোগ করে, **Manual Deploy → Clear build cache & deploy** দিন। এতে ১, ২, ৩
> নম্বর কারণ একসাথে বাদ পড়ে।

---

## যা বদলেছে এই রাউন্ডে

| ফাইল | পরিবর্তন |
|---|---|
| `routes/auth.py` | লাইভ টোকেন চেক **সবসময়** চলে (আর `None` নয়); "টোকেন মৃত" আর "টোকেন বৈধ কিন্তু ভুল মান" — দুটোর **আলাদা** বার্তা ও আলাদা পরামর্শ |
| `services/miniapp_auth.py` | নতুন `token_fingerprint()` — one-way sha256 digest, secret কখনো প্রকাশ নয় |
| `app.py` | `/health` → `telegram_token_fingerprint` |
| `services/pingbot.py` | বুটেই digest লগ করে + secret ছোট হলে "TRUNCATED" সতর্কতা |
| `tests/test_token_mismatch.py` | **নতুন** — ২০টি টেস্ট |

**নিরাপত্তা:** digest এক-মুখী, মাত্র ১২ hex অক্ষর, আর **auth পথে ব্যবহৃত হয় না** —
টেস্টে যাচাই করা। সঠিক পেলোড 200, tampered 400 — অপরিবর্তিত।

## টেস্ট

```
test_token_mismatch    20 passed, 0 failed   (নতুন)
test_badhash_evidence  23 passed, 0 failed
test_badhash_verdict   24 passed, 0 failed
test_miniapp_opens     33 passed, 0 failed
test_all_routes        65 pass,   0 fail
test_web_batch         24 pass,   0 fail
test_security_batch    22 pass,   0 fail
test_miniapp_auth     127 passed, 3 failed   (বেসলাইন — /code সংক্রান্ত)
test_telegram_link     78 passed, 1 failed   (বেসলাইন)
```

---

## নিরপেক্ষভাবে: আমার ভুলগুলোর হিসাব

| রাউন্ড | আমি বলেছিলাম | আসলে |
|---|---|---|
| ১ | "টোকেন পুরনো" | শর্তটা tautology ছিল |
| ২ | "পুরনো বটের বাটন" | `query_id` কোন বট সাইন করেছে তা বলে না |
| ৩ | "Revoke করুন" | টোকেন **বৈধ** — revoke করলে তৃতীয় টোকেন হতো, সমাধান নয় |

প্রতিবারই আমি **অনুমানকে সিদ্ধান্ত হিসেবে** ছেপেছি। এবার পার্থক্য হলো — এবার
প্রমাণ আছে (`tg_signature=VALID`), আর যেটুকু এখনো অজানা ছিল সেটা মাপার জন্য
fingerprint যোগ করা হয়েছে, যাতে **আপনি নিজেই** মিলিয়ে দেখতে পারেন।
